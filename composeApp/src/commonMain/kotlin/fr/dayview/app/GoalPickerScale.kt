package fr.dayview.app

/**
 * Scale applied to the goal date/time picker dialog. The Material3 calendar has a fixed
 * size that feels oversized with a mouse, so desktop shrinks it; touch platforms keep it
 * full-size for accessible tap targets.
 */
expect val goalPickerScale: Float
